# prop-it
Attach object properties with:
* custom getters / setters
* backing store
* change events
* readonly
* setter conversion function

## Installation

Install with npm

```shell
npm install prop-it
```

